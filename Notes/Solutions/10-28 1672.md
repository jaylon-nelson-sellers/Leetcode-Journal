```
class Solution:

    def maximumWealth(self, accounts: List[List[int]]) -> int:

        richest = 0

        temp = 0

        for account in accounts:

            temp = sum(account)

            if temp > richest:

                richest = temp

  

        return richest
```