# My Solution
```
class Solution:

    def runningSum(self, nums: List[int]) -> List[int]:

        runSum = len(nums) * [0]

        temp = 0

        for i in range(len(nums)):

            temp += nums[i]

            runSum[i] = temp

  

        return runSum
```

